export const DEFAULT_USER_ID = "default_user";
export const DEFAULT_GOAL = 3000; // ml

// Sleep tracking constants
export const DEFAULT_SLEEP_TARGET = 8; // hours
export const SLEEP_QUALITY_SCORES = {
  poor: 0.5,
  fair: 0.75,
  good: 1.0,
  excellent: 1.25,
};

// Gym tracking constants
export const DEFAULT_WEEKLY_GYM_TARGET = 4; // workouts per week
export const MUSCLE_GROUPS = [
  "chest",
  "back",
  "shoulders",
  "arms",
  "legs",
  "core",
  "cardio",
  "full-body",
];
export const WORKOUT_INTENSITY = {
  light: 0.7,
  moderate: 1.0,
  intense: 1.3,
};

// Diet tracking constants
export const DEFAULT_CALORIE_GOAL = 2000;
export const DEFAULT_PROTEIN_GOAL = 150; // grams
export const DEFAULT_CARBS_GOAL = 200; // grams
export const DEFAULT_FAT_GOAL = 65; // grams
